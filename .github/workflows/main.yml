on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: FTP Deploy Forked

    defaults:
      run:
        shell: bash -e {0}

    steps:
      - name: Set up Node.js
        uses: actions/setup-node@v4  # Use v4 or the latest version
        with:
          node-version: '20'

      - name: Checkout code
        uses: actions/checkout@v2

      - name: FTP Deploy
        uses: snorux/FTP-Deploy-Action@1.3.2
        with:
          server: crunchyapps.com
          username: ftpcicd@crunchyapps.com
          password: sTktLkcSax4B
          server-dir: /public_html/cicd/
